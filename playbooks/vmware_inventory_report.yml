---
- name: Gather facts from all VMs
  hosts: all
  gather_facts: false
  tasks:
    - name: print report
      debug:
        var: "{{ hostvars[inventory_name][item] }} - "
      loop:
      - config.name
      - guest.guestFullName
